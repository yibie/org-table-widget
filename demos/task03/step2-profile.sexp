
[profiler-profile "28.1" cpu #s(hash-table test equal data ([profiler-start progn condition-case org-table-widget-bench-main apply timer-event-handler nil nil nil nil nil nil nil nil nil nil] 1 [eval font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function 815>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure let org-table-widget--display-table if let while let save-excursion let] 1 [font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function 815>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure let org-table-widget--display-table if let while let save-excursion let progn] 8 [org-fontify-drawers font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function 815>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure let org-table-widget--display-table if let while let save-excursion let] 1 [re-search-forward org-activate-links font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function 815>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure let org-table-widget--display-table if let while let save-excursion] 2 [re-search-forward org-do-emphasis-faces font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function 815>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure let org-table-widget--display-table if let while let save-excursion] 2 [re-search-forward org-fontify-inline-src-blocks-1 org-fontify-inline-src-blocks font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function 815>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure let org-table-widget--display-table if let while let] 1 [buffer-local-value org-fold-core-get-folding-spec org-activate-folds font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function 815>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure let org-table-widget--display-table if let while let] 1 [nreverse let* let* while let let org-table-widget--clean-cell cons setq while let progn if save-excursion let org-table-widget--line-cells] 1 [string-trim let org-table-widget--clean-cell cons setq while let progn if save-excursion let org-table-widget--line-cells let cond while save-excursion] 4 [let* while let let org-table-widget--clean-cell cons setq while let progn if save-excursion let org-table-widget--line-cells let cond] 3 [text-properties-at let* while let let org-table-widget--clean-cell cons setq while let progn if save-excursion let org-table-widget--line-cells let] 1 [let org-table-widget--clean-cell cons setq while let progn if save-excursion let org-table-widget--line-cells let cond while save-excursion let] 2 [let* let* while let let org-table-widget--clean-cell cons setq while let progn if save-excursion let org-table-widget--line-cells let] 2 [cdr setq while let* let* while let let org-table-widget--clean-cell cons setq while let progn if save-excursion] 1 [progn while let* let* while let let org-table-widget--clean-cell cons setq while let progn if save-excursion let] 1 [cdr setq prog1 car-safe setq progn while let* let* while let let org-table-widget--clean-cell cons setq while] 1 [buffer-substring org-table-widget--clean-cell cons setq while let progn if save-excursion let org-table-widget--line-cells let cond while save-excursion let] 1 [if "#<interpreted-function 265>" mapcar apply let* progn if let org-table-widget--parse and let* let org-table-widget--display-table if let while] 1 [aref memq cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if let while] 3 [insert let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 57D>" funcall puthash if let org-table-widget--measure-string let while let*] 2 [window-text-pixel-size car setq let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 57D>" funcall puthash if let org-table-widget--measure-string max] 90 [window-text-pixel-size car setq let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 57D>" funcall puthash if let org-table-widget--measure-string let] 95 [cconv-fv cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths let*] 2 [let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let* let* let] 1 [equal-including-properties gethash let org-table-widget--measure-string max setq let while let let while let* if let while let] 4 [while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths let*] 2 [if let org-table-widget--measure-string max setq let while let let while let* if let while let let*] 3 [cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths let* org-table-widget--render] 4 [copy-sequence let "#<interpreted-function 57D>" funcall puthash if let org-table-widget--measure-string let while let* if let while let let*] 1 [if let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let let*] 3 [let save-current-buffer let "#<interpreted-function 57D>" funcall puthash if let org-table-widget--measure-string max setq progn if "#<interpreted-function D10>" funcall progn] 4 [let* let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let* let*] 1 [aref org-table-widget--regional-indicator-p and if let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let] 3 [let save-current-buffer let "#<interpreted-function 57D>" funcall puthash if let org-table-widget--measure-string let while let* if let while let] 4 [remove-text-properties let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 57D>" funcall puthash if let org-table-widget--measure-string let while let*] 2 [cconv--analyze-function cconv-analyze-form cconv-fv cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let while let let*] 3 [cdr setq while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let* let*] 1 [org-table-widget--measure-string max setq let while let let while let* if let while let let* org-table-widget--widths let*] 5 [let org-table-widget--grapheme-ends let let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect] 2 [org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let* let*] 2 [org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while] 5 [org-table-widget--regional-indicator-p and if let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while] 3 [cconv-analyze-form cconv-analyze-form cconv-analyze-form cconv--analyze-function cconv-analyze-form cconv-fv cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let] 1 [aref and org-table-widget--break-after-p and cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if] 1 [eq if let org-table-widget--measure-string max setq let while let let while let* if let while let] 2 [let while let let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect] 5 ["#<native-comp-function F616e6f6e796d6f75732d6c616d626461_anonymous_lambda_28>" cconv-fv cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths] 2 [while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let* let* let org-table-widget--layout] 1 [let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let let* org-table-widget--widths] 4 [equal-including-properties gethash let org-table-widget--measure-string max setq progn if "#<interpreted-function 8B2>" funcall cond let while let progn let*] 1 [cconv-analyze-form cconv-analyze-form cconv-analyze-form cconv-analyze-form cconv--analyze-function cconv-analyze-form cconv-fv cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if] 1 [< while let org-table-widget--grapheme-ends let let while let* if let while let let* org-table-widget--widths let* org-table-widget--render] 2 [gethash let org-table-widget--measure-string max setq let while let let while let* if let while let let*] 8 [aref org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let] 1 [org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let let* org-table-widget--widths let*] 3 [> if "#<interpreted-function D09>" funcall cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if] 1 [< while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths] 1 [1+ setq while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let let*] 1 [cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let let*] 2 [sxhash-equal-including-properties gethash let org-table-widget--measure-string let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn] 2 [sxhash-equal-including-properties gethash let org-table-widget--measure-string max setq let while let let while let* if let while let] 3 [sxhash-equal-including-properties gethash let org-table-widget--measure-string max setq progn if "#<interpreted-function 4A9>" funcall progn let* let org-table-widget--longest-token-pixels let while] 1 [setq let while let let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn] 6 ["#<interpreted-function 57D>" funcall puthash if let org-table-widget--measure-string let while let* if let while let let* org-table-widget--widths let*] 2 [+ setq while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let* let*] 1 [macroexp--cons macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let while let let*] 1 [length let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let let*] 2 [let while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths] 5 [delete-region let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 57D>" funcall puthash if let org-table-widget--measure-string max setq progn] 1 [let org-table-widget--measure-string max setq let while let let while let* if let while let let* org-table-widget--widths] 3 [save-current-buffer let "#<interpreted-function 57D>" funcall puthash if let org-table-widget--measure-string max setq progn if "#<interpreted-function 6A8>" funcall progn let*] 1 [funcall puthash if let org-table-widget--measure-string max setq progn if "#<interpreted-function E2A>" funcall progn let* let org-table-widget--longest-token-pixels let] 1 [substring org-table-widget--measure-string max setq let while let let while let* if let while let let* org-table-widget--widths] 9 [macroexpand-1 macroexp-macroexpand macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let] 1 [quote eq if let org-table-widget--measure-string max setq let while let let while let* if let while] 2 [put-text-property let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 57D>" funcall puthash if let org-table-widget--measure-string let while let*] 3 [macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let*] 3 [cconv-analyze-form cconv-analyze-form cconv--analyze-function cconv-analyze-form cconv-fv cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let while] 1 [<= org-table-widget--regional-indicator-p and if let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let] 2 [max + max setcar let* let while let* if let while let let* org-table-widget--widths let* org-table-widget--render] 1 [let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 57D>" funcall puthash if let org-table-widget--measure-string max setq progn if] 1 [= or org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if] 1 [cons setq while let org-table-widget--grapheme-ends let let while let* if let while let let* org-table-widget--widths let*] 1 [buffer-modified-p let save-current-buffer let "#<interpreted-function 57D>" funcall puthash if let org-table-widget--measure-string max setq progn if "#<interpreted-function 6F5>" funcall] 1 [let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let* let* let org-table-widget--layout funcall] 1 [save-restriction save-excursion let save-current-buffer let "#<interpreted-function 57D>" funcall puthash if let org-table-widget--measure-string let while let* if let] 1 [if let org-table-widget--measure-string let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect] 1 [char-category-set aref and org-table-widget--break-after-p and cond let while let progn let* let org-table-widget--longest-token-pixels let while let*] 1 [cconv-analyze-form cconv-analyze-form cconv-analyze-form cconv-analyze-form cconv-analyze-form cconv-analyze-form cconv--analyze-function cconv-analyze-form cconv-fv cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while] 1 [puthash if let org-table-widget--measure-string max setq progn if "#<interpreted-function DA9>" funcall progn let* let org-table-widget--longest-token-pixels let while] 1 [max setq let while let let while let* if let while let let* org-table-widget--widths let* org-table-widget--render] 2 [eq if let org-table-widget--measure-string max setq progn if "#<interpreted-function EA9>" funcall progn let* let org-table-widget--longest-token-pixels let while] 1 [eq if let org-table-widget--measure-string let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn] 2 [length < while let org-table-widget--grapheme-ends let let while let* if let while let let* org-table-widget--widths let*] 1 [and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let] 3 [setcar let* let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let*] 1 [char-width > and org-table-widget--break-after-p and cond let while let progn let* let org-table-widget--longest-token-pixels let while let*] 1 [insert let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 57D>" funcall puthash if let org-table-widget--measure-string max setq progn] 2 [setq while let org-table-widget--grapheme-ends let let while let* if let while let let* org-table-widget--widths let* org-table-widget--render] 1 [point-marker let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 57D>" funcall puthash if let org-table-widget--measure-string max setq progn] 1 [aref char-category-set aref and org-table-widget--break-after-p and cond let while let progn let* let org-table-widget--longest-token-pixels let while] 1 [and if let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let] 2 [max setq progn if "#<interpreted-function 507>" funcall progn let* let org-table-widget--longest-token-pixels let while let* if let while] 2 [car-safe cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths let*] 1 [char-width = or org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let*] 1 [cconv-analyze-form cconv--analyze-function cconv-analyze-form cconv-fv cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let while let] 1 [and cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let] 2 [min max + max setcar let* let while let* if let while let let* org-table-widget--widths let*] 1 [aref char-width > and org-table-widget--break-after-p and cond let while let progn let* let org-table-widget--longest-token-pixels let while] 1 [save-excursion let save-current-buffer let "#<interpreted-function 57D>" funcall puthash if let org-table-widget--measure-string let while let* if let while] 1 [substring org-table-widget--measure-string max setq progn if "#<interpreted-function BA9>" funcall progn let* let org-table-widget--longest-token-pixels let while let* if] 1 [macroexpand-1 macroexp-macroexpand macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let*] 1 [macroexp-macroexpand macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all] 1 [while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let let*] 1 [copy-sequence puthash if let org-table-widget--measure-string let while let* if let while let let* org-table-widget--widths let* org-table-widget--render] 1 [setq progn if "#<interpreted-function FC0>" funcall progn let* let org-table-widget--longest-token-pixels let while let* if let while let] 1 [propertize if org-table-widget--pixel-space concat propertize let* org-table-widget--rule-segment "#<interpreted-function F32>" mapconcat concat org-table-widget--rule apply list let* org-table-widget--render progn] 1 [equal-including-properties gethash let org-table-widget--measure-string let* org-table-widget--pad let* "#<interpreted-function 4DF>" apply seq-mapn let let while let let* org-table-widget--row] 1 [setq let* "#<interpreted-function 3DF>" apply seq-mapn let let while let let* org-table-widget--row cons setq let* if let] 1 [window-font-width progn condition-case list let save-current-buffer org-table-widget--font-signature let save-current-buffer org-table-widget--char-pixel-width let* org-table-widget--row cons setq let* if] 4 [let* org-table-widget--pad let* "#<interpreted-function 4AA>" apply seq-mapn let let while let let* org-table-widget--row cons setq let* if] 3 [cconv-make-interpreted-closure function mapcar let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render progn unwind-protect] 3 [seq-map seq-mapn let let while let let* org-table-widget--row cons setq let* if let while let let*] 1 [cconv-make-interpreted-closure function seq-mapn let let while let let* org-table-widget--row cons setq let* if let while let] 1 [concat cons setq let let while let let* org-table-widget--row cons setq let* if let while let] 5 [while let let* org-table-widget--render progn unwind-protect let* let* let org-table-widget--layout funcall let let textui-layout-widget let* if] 1 [macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function seq-mapn let] 1 [concat let* "#<interpreted-function 60E>" apply seq-mapn let let while let let* org-table-widget--row cons setq let* if let] 3 [let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends vconcat let* org-table-widget--fit-end let* while let if org-table-widget--wrap-pixels "#<interpreted-function 350>" apply] 1 [org-table-widget--wrap-pixels "#<interpreted-function 6E7>" apply seq-mapn let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render progn] 1 [mapconcat string-join concat cons setq let let while let let* org-table-widget--row cons setq let* if let] 1 [car mapcar "#<byte-code-function D95>" apply seq-map seq-mapn let let while let let* org-table-widget--row cons setq let* if] 1 [macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function seq-mapn let let while let let* org-table-widget--row cons] 1 [window-text-pixel-size car setq let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 57D>" funcall puthash if let org-table-widget--measure-string let*] 2 [equal-including-properties gethash let org-table-widget--measure-string <= or if org-table-widget--wrap-pixels "#<interpreted-function 33F>" apply seq-mapn let* org-table-widget--row cons setq let*] 3 [macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function seq-mapn let let while let let*] 1 [concat let org-table-widget--boundary let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render progn unwind-protect] 1 [funcall puthash if let org-table-widget--measure-string let* while let* org-table-widget--fit-end let* while let if org-table-widget--wrap-pixels "#<interpreted-function 426>" apply] 1 [mapconcat string-join let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render progn unwind-protect let*] 3 [cconv-fv cconv-make-interpreted-closure function mapcar let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render progn] 1 [concat let* org-table-widget--pad let* "#<interpreted-function 40E>" apply seq-mapn let let while let let* org-table-widget--row cons setq let*] 2 [= or org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends vconcat let* org-table-widget--fit-end let* while] 1 [if let org-table-widget--measure-string let* org-table-widget--pad let* "#<interpreted-function D42>" apply seq-mapn let let while let let* org-table-widget--row cons] 1 [cconv-analyze-form cconv-analyze-form cconv-analyze-form cconv-analyze-form cconv--analyze-function cconv-analyze-form cconv-fv cconv-make-interpreted-closure function seq-mapn let let while let let* org-table-widget--row] 1 [apply seq-map seq-mapn let let while let let* org-table-widget--row cons setq let* if let while let] 1 [let org-table-widget--measure-string <= or if org-table-widget--wrap-pixels "#<interpreted-function 068>" apply seq-mapn let* org-table-widget--row cons setq let* if let] 1 [let* "#<interpreted-function B2B>" apply seq-mapn let let while let let* org-table-widget--row cons setq let* if let while] 1 [org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends vconcat let* org-table-widget--fit-end let* while let if org-table-widget--wrap-pixels "#<interpreted-function 93A>" apply seq-mapn] 2 [string-empty-p or if org-table-widget--wrap-pixels "#<interpreted-function 63A>" apply seq-mapn let* org-table-widget--row cons setq let* if let while let] 1 [condition-case list let save-current-buffer org-table-widget--font-signature let save-current-buffer org-table-widget--char-pixel-width let* org-table-widget--row cons setq let* if let while] 1 [cond org-table-widget--padding-widths let* org-table-widget--pad let* "#<interpreted-function 217>" apply seq-mapn let let while let let* org-table-widget--row cons setq] 2 [= and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends vconcat let* org-table-widget--fit-end let* while let if] 1 [macroexp-macroexpand macroexp--expand-all macroexp--all-forms macroexp--all-clauses macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function seq-mapn let let while let let*] 1 [org-table-widget--regional-indicator-p and if let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends vconcat let* org-table-widget--fit-end let* while let if] 1 [macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--all-clauses macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function seq-mapn let let while let] 1 [macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function seq-mapn let* org-table-widget--row cons setq let* if let] 1 [mapcar "#<byte-code-function D95>" apply seq-map seq-mapn let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render] 2 [quote eq if let org-table-widget--measure-string <= or if org-table-widget--wrap-pixels "#<interpreted-function 50B>" apply seq-mapn let* org-table-widget--row cons setq] 1 [macroexpand-1 macroexp-macroexpand macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function mapcar let* org-table-widget--row cons setq let* if let while let] 1 [* - max "#<interpreted-function 548>" mapcar let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render] 1 [nth or let* "#<interpreted-function 453>" apply seq-mapn let let while let let* org-table-widget--row cons setq let* if] 1 [seq-mapn let let while let let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render] 1 [seq-into "#<byte-code-function 63D>" mapcar "#<byte-code-function D95>" apply seq-map seq-mapn let* org-table-widget--row cons setq let* if let while let] 1 [let save-current-buffer org-table-widget--char-pixel-width let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render progn unwind-protect] 1 ["#<native-comp-function F616e6f6e796d6f75732d6c616d626461_anonymous_lambda_30>" cconv-make-interpreted-closure function mapcar let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render progn] 1 [mapcar "#<byte-code-function D95>" apply seq-map seq-mapn let let while let let* org-table-widget--row cons setq let* if let] 1 [function-get macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function mapcar let* org-table-widget--row cons setq let* if let while] 1 [macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function mapcar let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render] 1 [goto-char save-restriction save-excursion let save-current-buffer let "#<interpreted-function 57D>" funcall puthash if let org-table-widget--measure-string let* org-table-widget--pad let* "#<interpreted-function 61C>"] 1 [1+ let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends vconcat let* org-table-widget--fit-end let* while let if org-table-widget--wrap-pixels "#<interpreted-function E0E>"] 1 [max let* org-table-widget--pad let* "#<interpreted-function 25E>" apply seq-mapn let let while let let* org-table-widget--row cons setq let*] 1 [org-table-widget--boundary let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render progn unwind-protect let* let*] 1 [mapconcat string-join let let* org-table-widget--render progn unwind-protect let* let* let org-table-widget--layout funcall let let textui-layout-widget let*] 4 [add-face-text-property let let* org-table-widget--render progn unwind-protect let* let* let org-table-widget--layout funcall let let textui-layout-widget let* if] 1 [concat if overlay-put let* if let* let org-table-widget--display-table if let while let save-excursion let progn if] 5 [Automatic\ GC nil] 104)) (27294 32209 448660 0) nil]
