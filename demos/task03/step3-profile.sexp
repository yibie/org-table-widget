
[profiler-profile "28.1" cpu #s(hash-table test equal data ([profiler-start progn condition-case org-table-widget-bench-main apply timer-event-handler nil nil nil nil nil nil nil nil nil nil] 1 [font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function 415>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure let org-table-widget--display-table if let while let save-excursion let progn] 8 [re-search-forward org-activate-links font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function 415>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure let org-table-widget--display-table if let while let save-excursion] 1 [org-activate-dates font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function 415>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure let org-table-widget--display-table if let while let save-excursion let] 1 [org-activate-code font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function 415>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure let org-table-widget--display-table if let while let save-excursion let] 1 [re-search-forward org-fontify-inline-src-blocks-1 org-fontify-inline-src-blocks font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function 415>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure let org-table-widget--display-table if let while let] 1 [gethash org-fold-core--property-symbol-get-create org-fold-core-get-folding-spec org-activate-folds font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function 415>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure let org-table-widget--display-table if let while] 1 [org-fold-core--property-symbol-get-create org-fold-core-get-folding-spec org-activate-folds font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function 415>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure let org-table-widget--display-table if let while let] 2 [secure-hash list let* let org-table-widget--display-table if let while let save-excursion let progn if let org-table-widget-refresh progn] 1 [line-end-position let org-table-widget--line-cells let cond while save-excursion let org-table-widget--parse and let* if let* let org-table-widget--display-table if] 3 [cconv-make-interpreted-closure function seq-some and org-table-widget--cookie-row-p cond let cond while save-excursion let org-table-widget--parse and let* if let*] 2 [setq prog1 car-safe setq progn while let* let* while let let org-table-widget--clean-cell cons setq while let] 1 [let* let* while let let org-table-widget--clean-cell cons setq while let progn if save-excursion let org-table-widget--line-cells let] 1 [consp while let* let* while let let org-table-widget--clean-cell cons setq while let progn if save-excursion let] 1 [setq let* while let let org-table-widget--clean-cell cons setq while let progn if save-excursion let org-table-widget--line-cells let] 1 [setq while let* let* while let let org-table-widget--clean-cell cons setq while let progn if save-excursion let] 1 [cdr cdr setq while let* let* while let let org-table-widget--clean-cell cons setq while let progn if] 2 [buffer-substring org-table-widget--clean-cell cons setq while let progn if save-excursion let org-table-widget--line-cells let cond while save-excursion let] 1 [string-match string-match-p "#<interpreted-function 1DB>" "#<byte-code-function 1E7>" mapc seq-do seq-some and org-table-widget--cookie-row-p cond let cond while save-excursion let org-table-widget--parse] 1 [string-trim let org-table-widget--clean-cell cons setq while let progn if save-excursion let org-table-widget--line-cells let cond while save-excursion] 1 [re-search-forward while let progn if save-excursion let org-table-widget--line-cells let cond while save-excursion let org-table-widget--parse and let*] 2 [cons setq while let progn if save-excursion let org-table-widget--line-cells let cond while save-excursion let org-table-widget--parse and] 1 [point setq while let progn if save-excursion let org-table-widget--line-cells let cond while save-excursion let org-table-widget--parse and] 1 ["#<interpreted-function F10>" mapcar apply let* progn if let org-table-widget--parse and let* if let* let org-table-widget--display-table if let] 1 [window-text-pixel-size car progn unwind-protect let save-restriction save-excursion let save-current-buffer let "#<interpreted-function E44>" funcall puthash if let org-table-widget--measure-string] 161 [and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let] 5 [setq while let org-table-widget--grapheme-ends let let while let* if let while let let* org-table-widget--widths let* org-table-widget--render] 1 [let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect] 2 [let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let let* org-table-widget--widths] 4 [let save-current-buffer let "#<interpreted-function E44>" funcall puthash if let org-table-widget--measure-string let while let* if let while let] 2 [aref org-table-widget--regional-indicator-p and if let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let] 1 [add-face-text-property let "#<interpreted-function E44>" funcall puthash if let org-table-widget--measure-string max setq progn if "#<interpreted-function D78>" funcall progn let*] 2 [org-table-widget--measure-string max setq let while let let while let* if let while let let* org-table-widget--widths let*] 3 [copy-sequence let "#<interpreted-function E44>" funcall puthash if let org-table-widget--measure-string let while let* if let while let let*] 1 [org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while] 4 [setq let while let let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn] 2 [memq cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let] 1 [org-table-widget--regional-indicator-p and if let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while] 3 [cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let let*] 1 [org-table-widget--break-after-p and cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if let while] 3 [macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let*] 3 ["#<native-comp-function F616e6f6e796d6f75732d6c616d626461_anonymous_lambda_28>" cconv-fv cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths] 2 [gethash let org-table-widget--measure-string max setq let while let let while let* if let while let let*] 9 [substring org-table-widget--measure-string max setq let while let let while let* if let while let let* org-table-widget--widths] 7 [let while let let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect] 3 [let while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths] 5 [if let org-table-widget--measure-string max setq progn if "#<interpreted-function 9FE>" funcall progn let* let org-table-widget--longest-token-pixels let while let*] 1 [insert progn unwind-protect let save-restriction save-excursion let save-current-buffer let "#<interpreted-function E44>" funcall puthash if let org-table-widget--measure-string let] 2 [= and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while] 3 [cconv-analyze-form cconv-analyze-form cconv-analyze-form cconv-analyze-form cconv--analyze-function cconv-analyze-form cconv-fv cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if] 1 [org-table-widget--measure-string let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let* let*] 3 [macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function] 1 [gethash let org-table-widget--measure-string max setq progn if "#<interpreted-function 47B>" funcall cond let while let progn let* let] 4 [aref char-category-set aref and org-table-widget--break-after-p and cond let while let progn let* let org-table-widget--longest-token-pixels let while] 1 [and cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let] 4 [funcall puthash if let org-table-widget--measure-string let while let* if let while let let* org-table-widget--widths let* org-table-widget--render] 1 [save-restriction save-excursion let save-current-buffer let "#<interpreted-function E44>" funcall puthash if let org-table-widget--measure-string max setq progn if "#<interpreted-function F1D>"] 1 [let org-table-widget--measure-string max setq let while let let while let* if let while let let* org-table-widget--widths] 6 [< while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths] 3 [let org-table-widget--grapheme-ends let let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect] 1 [aref memq cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if let while] 2 [< and cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if let while] 3 [let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let* let* let] 3 [1- < and cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if let] 3 [equal-including-properties gethash let org-table-widget--measure-string max setq progn if "#<interpreted-function 0F1D>" funcall progn let* let org-table-widget--longest-token-pixels let while] 1 [macroexp-macroexpand macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while] 1 [gethash let org-table-widget--measure-string let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect] 2 [substring org-table-widget--measure-string max setq progn if "#<interpreted-function D48>" funcall progn let* let org-table-widget--longest-token-pixels let while let* if] 2 [max setq let while let let while let* if let while let let* org-table-widget--widths let* org-table-widget--render] 5 [consp while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let* let* let] 1 [quote memq cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if let while] 1 [setq while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths] 2 [while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let let*] 2 [if let org-table-widget--measure-string max setq let while let let while let* if let while let let*] 1 [unwind-protect let save-restriction save-excursion let save-current-buffer let "#<interpreted-function E44>" funcall puthash if let org-table-widget--measure-string let while let*] 1 [let org-table-widget--measure-string let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let*] 1 [org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let* let*] 1 [<= org-table-widget--regional-indicator-p and if let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let] 2 [cconv-fv cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths let*] 2 [= or org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if] 1 [max setq progn if "#<interpreted-function 70A>" funcall progn let* let org-table-widget--longest-token-pixels let while let* if let while] 1 [cdr setq let while let let while let* if let while let let* org-table-widget--widths let* org-table-widget--render] 1 [sxhash-equal-including-properties gethash let org-table-widget--measure-string max setq let while let let while let* if let while let] 1 [substring org-table-widget--measure-string max setq progn if "#<interpreted-function 9E0>" funcall cond let while let progn let* let org-table-widget--longest-token-pixels] 1 [max setcar let* let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect] 1 [1+ setq while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let let*] 1 [macroexpand-1 macroexp-macroexpand macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all] 1 [<= or org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if] 1 [> if "#<interpreted-function CA9>" funcall progn let* let org-table-widget--longest-token-pixels let while let* if let while let let*] 1 [macroexpand-1 macroexp-macroexpand macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure] 1 [cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths let* org-table-widget--render] 2 [quote eq if let org-table-widget--measure-string max setq let while let let while let* if let while] 1 [cons setq while let org-table-widget--grapheme-ends let let while let* if let while let let* org-table-widget--widths let*] 4 [let save-current-buffer let "#<interpreted-function E44>" funcall puthash if let org-table-widget--measure-string max setq progn if "#<interpreted-function A78>" funcall progn] 5 [equal-including-properties gethash let org-table-widget--measure-string max setq let while let let while let* if let while let] 2 [org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let let* org-table-widget--widths let*] 1 [< and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while] 2 [equal-including-properties gethash let org-table-widget--measure-string max setq progn if "#<interpreted-function 552>" funcall cond let while let progn let*] 1 [and if let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let] 1 [length < while let org-table-widget--grapheme-ends let let while let* if let while let let* org-table-widget--widths let*] 1 [function-get macroexp--expand-all macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let] 1 [eq if let org-table-widget--measure-string max setq let while let let while let* if let while let] 2 ["#<byte-code-function 035>" cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths let*] 1 [let* let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let* let*] 1 [funcall progn let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths let* org-table-widget--render] 1 [save-excursion let save-current-buffer let "#<interpreted-function E44>" funcall puthash if let org-table-widget--measure-string max setq progn if "#<interpreted-function 484>" funcall] 1 [> if "#<interpreted-function 70D>" funcall cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if] 1 [if let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let let*] 1 [delete-region progn if unwind-protect let save-restriction save-excursion let save-current-buffer let "#<interpreted-function E44>" funcall puthash if let org-table-widget--measure-string] 2 [macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths] 1 [aref org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let] 1 [max org-table-widget--pixel-space concat let org-table-widget--boundary let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render] 1 [org-table-widget--regional-indicator-p and if let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends vconcat let* org-table-widget--fit-end let* while let if] 3 [mapconcat string-join let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render progn unwind-protect let*] 3 [macroexp-macroexpand macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--all-clauses macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function seq-mapn let let while] 1 [concat let* org-table-widget--pad let* "#<interpreted-function D6A>" apply seq-mapn let let while let let* org-table-widget--row cons setq let*] 3 ["#<native-comp-function F616e6f6e796d6f75732d6c616d626461_anonymous_lambda_28>" cconv-fv cconv-make-interpreted-closure function mapcar let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render] 2 [let* while let* org-table-widget--fit-end let* while let if org-table-widget--wrap-pixels "#<interpreted-function 7E4>" apply seq-mapn let* org-table-widget--row cons setq] 1 [let* org-table-widget--pad let* "#<interpreted-function 9FF>" apply seq-mapn let let while let let* org-table-widget--row cons setq let* if] 2 [eq if let org-table-widget--measure-string let* org-table-widget--pad let* "#<interpreted-function 440>" apply seq-mapn let let while let let* org-table-widget--row] 1 [macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--all-clauses macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function seq-mapn let let while let let*] 1 [aref = and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends vconcat let* org-table-widget--fit-end let* while let] 1 [= or org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends vconcat let* org-table-widget--fit-end let* while] 1 [mapconcat string-join concat cons setq let let while let let* org-table-widget--row cons setq let* if let] 2 [concat let* "#<interpreted-function 009>" apply seq-mapn let let while let let* org-table-widget--row cons setq let* if let] 1 [and cond let* if let while let let* org-table-widget--render progn unwind-protect let* let* let org-table-widget--layout funcall] 1 [cconv-make-interpreted-closure function seq-mapn let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render progn unwind-protect] 2 [window-font-width progn condition-case list let save-current-buffer org-table-widget--font-signature let save-current-buffer org-table-widget--char-pixel-width let* org-table-widget--row cons setq let* if] 3 [macroexp--all-forms macroexp--all-clauses macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function seq-mapn let let while let let* org-table-widget--row cons] 1 [let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends vconcat let* org-table-widget--fit-end let* while let if org-table-widget--wrap-pixels "#<interpreted-function 968>" apply] 1 [put-text-property progn unwind-protect let save-restriction save-excursion let save-current-buffer let "#<interpreted-function E44>" funcall puthash if let org-table-widget--measure-string let*] 1 [> if org-table-widget--pixel-space concat let* org-table-widget--pad let* "#<interpreted-function 51D>" apply seq-mapn let let while let let* org-table-widget--row] 1 [let org-table-widget--measure-string <= or if org-table-widget--wrap-pixels "#<interpreted-function 13A>" apply seq-mapn let* org-table-widget--row cons setq let* if let] 3 [cconv-make-interpreted-closure function seq-mapn let let while let let* org-table-widget--row cons setq let* if let while let] 1 [- max let* org-table-widget--pad let* "#<interpreted-function A0E>" apply seq-mapn let let while let let* org-table-widget--row cons setq] 1 [mapcar "#<byte-code-function 995>" apply seq-map seq-mapn let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render] 1 [cond org-table-widget--padding-widths let* org-table-widget--pad let* "#<interpreted-function E40>" apply seq-mapn let let while let let* org-table-widget--row cons setq] 1 [concat cons setq let let while let let* org-table-widget--row cons setq let* if let while let] 1 [add-face-text-property progn if let* "#<interpreted-function 40E>" apply seq-mapn let let while let let* org-table-widget--row cons setq let*] 1 [car-safe cconv-make-interpreted-closure function seq-mapn let let while let let* org-table-widget--row cons setq let* if let while] 1 [aref org-table-widget--regional-indicator-p and if let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends vconcat let* org-table-widget--fit-end let* while let] 1 [cconv-make-interpreted-closure function mapcar let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render progn unwind-protect] 1 [1+ let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends vconcat let* org-table-widget--fit-end let* while let if org-table-widget--wrap-pixels "#<interpreted-function AE8>"] 1 [let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render progn unwind-protect let* let* let] 1 [condition-case list let save-current-buffer org-table-widget--font-signature let save-current-buffer org-table-widget--char-pixel-width let* org-table-widget--row cons setq let* if let while] 1 [mapconcat string-join let let* org-table-widget--render progn unwind-protect let* let* let org-table-widget--layout funcall let let textui-layout-widget let*] 4 [concat if list setq let* if let* if let* let org-table-widget--display-table if let while let save-excursion] 5 [Automatic\ GC nil] 69)) (27294 33175 904096 0) nil]
