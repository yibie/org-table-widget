
[profiler-profile "28.1" cpu #s(hash-table test equal data ([profiler-start progn condition-case org-table-widget-bench-main apply timer-event-handler nil nil nil nil nil nil nil nil nil nil] 2 [font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function D10>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure org-table-widget--display-table if let while let save-excursion let progn if] 3 [re-search-forward org-activate-links font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function D10>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure org-table-widget--display-table if let while let save-excursion let] 1 [re-search-forward org-do-emphasis-faces font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function D10>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure org-table-widget--display-table if let while let save-excursion let] 3 [org-activate-code font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function D10>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure org-table-widget--display-table if let while let save-excursion let progn] 1 [org-cite-activate font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function D10>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure org-table-widget--display-table if let while let save-excursion let progn] 1 [search-forward org-activate-folds font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function D10>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure org-table-widget--display-table if let while let save-excursion let] 1 [line-end-position let org-table-widget--line-cells let cond while save-excursion let org-table-widget--parse and let* org-table-widget--display-table if let while let] 2 [setq progn while let* let* while let let org-table-widget--clean-cell cons setq while let progn if save-excursion] 2 [string-trim let org-table-widget--clean-cell cons setq while let progn if save-excursion let org-table-widget--line-cells let cond while save-excursion] 4 [buffer-substring org-table-widget--clean-cell cons setq while let progn if save-excursion let org-table-widget--line-cells let cond while save-excursion let] 1 [progn if save-excursion let org-table-widget--line-cells let cond while save-excursion let org-table-widget--parse and let* org-table-widget--display-table if let] 1 [make-closure seq-some and org-table-widget--cookie-row-p cond let cond while save-excursion let org-table-widget--parse and let* org-table-widget--display-table if let] 1 [cdr cdr setq while let* let* while let let org-table-widget--clean-cell cons setq while let progn if] 1 [while let* let* while let let org-table-widget--clean-cell cons setq while let progn if save-excursion let org-table-widget--line-cells] 1 [next-property-change or let* while let let org-table-widget--clean-cell cons setq while let progn if save-excursion let org-table-widget--line-cells] 1 [while let progn if save-excursion let org-table-widget--line-cells let cond while save-excursion let org-table-widget--parse and let* org-table-widget--display-table] 1 [save-excursion let org-table-widget--line-cells let cond while save-excursion let org-table-widget--parse and let* org-table-widget--display-table if let while let] 1 [remove-list-of-text-properties progn if let* while let let org-table-widget--clean-cell cons setq while let progn if save-excursion let] 1 [cconv-analyze-form cconv--analyze-function cconv-analyze-form cconv-fv cconv-make-interpreted-closure function seq-some and org-table-widget--cookie-row-p cond let cond while save-excursion let org-table-widget--parse] 1 [prog1 car-safe setq progn while let* let* while let let org-table-widget--clean-cell cons setq while let progn] 1 [setq let* while let let org-table-widget--clean-cell cons setq while let progn if save-excursion let org-table-widget--line-cells let] 1 [looking-at looking-at-p cond while save-excursion let org-table-widget--parse and let* org-table-widget--display-table if let while let save-excursion let] 1 [org-table-widget--line-cells let cond while save-excursion let org-table-widget--parse and let* org-table-widget--display-table if let while let save-excursion let] 1 [let while let let let org-table-widget--parse and let* org-table-widget--display-table if let while let save-excursion let progn] 1 [aref org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let] 3 [setq while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths] 2 [window-text-pixel-size car setq let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 93E>" funcall puthash if let org-table-widget--measure-string max] 70 [org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let let* org-table-widget--widths let*] 2 [quote remove-text-properties let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 93E>" funcall puthash if let org-table-widget--measure-string let while] 1 [gethash let org-table-widget--measure-string max setq let while let let while let* if let while let let*] 5 [let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let let* org-table-widget--widths] 5 [window-text-pixel-size car setq let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 93E>" funcall puthash if let org-table-widget--measure-string let] 79 [org-table-widget--measure-string let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let* let*] 3 [macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function let*] 2 [and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let] 2 [macroexpand-1 macroexp-macroexpand macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function let*] 1 [macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure] 1 [let save-current-buffer let "#<interpreted-function 93E>" funcall puthash if let org-table-widget--measure-string let while let* if let while let] 5 [= or org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if] 1 [macroexp--all-forms macroexp--expand-all macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let] 2 [let org-table-widget--measure-string max setq let while let let while let* if let while let let* org-table-widget--widths] 5 [goto-char save-restriction save-excursion let save-current-buffer let "#<interpreted-function 93E>" funcall puthash if let org-table-widget--measure-string max setq progn if] 2 [let "#<interpreted-function 93E>" funcall puthash if let org-table-widget--measure-string let while let* if let while let let* org-table-widget--widths] 2 [sxhash-equal-including-properties gethash let org-table-widget--measure-string max setq progn if "#<interpreted-function ED0>" funcall progn let* let org-table-widget--longest-token-pixels let while] 1 [let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let* let* let] 3 [org-table-widget--measure-string max setq let while let let while let* if let while let let* org-table-widget--widths let*] 3 [substring org-table-widget--measure-string max setq progn if "#<interpreted-function 89D>" funcall cond let while let progn let* let org-table-widget--longest-token-pixels] 7 [let while let let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect] 2 [equal-including-properties gethash let org-table-widget--measure-string max setq let while let let while let* if let while let] 4 [eq if let org-table-widget--measure-string max setq let while let let while let* if let while let] 1 [substring org-table-widget--measure-string max setq let while let let while let* if let while let let* org-table-widget--widths] 41 [while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths let*] 2 [setq let while let let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn] 3 [gethash let org-table-widget--measure-string max setq progn if "#<interpreted-function D35>" funcall progn let* let org-table-widget--longest-token-pixels let while let*] 1 [insert let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 93E>" funcall puthash if let org-table-widget--measure-string let while let*] 3 [set-marker let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 93E>" funcall puthash if let org-table-widget--measure-string let while let*] 1 [org-table-widget--regional-indicator-p and if let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while] 2 [org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while] 2 [puthash if let org-table-widget--measure-string let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn] 5 [set-marker-insertion-type let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 93E>" funcall puthash if let org-table-widget--measure-string let while let*] 1 [1+ let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let let*] 1 [car let while let let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn] 1 [aref and org-table-widget--break-after-p and cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if] 1 [cons setq while let org-table-widget--grapheme-ends let let while let* if let while let let* org-table-widget--widths let*] 3 [quote gethash let org-table-widget--measure-string let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn] 1 [if let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let let*] 3 [< and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while] 3 [quote gethash let org-table-widget--measure-string max setq let while let let while let* if let while let] 1 [aref org-table-widget--regional-indicator-p and if let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let] 2 [char-width = or org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let*] 1 [let progn let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths let* org-table-widget--render] 1 [point-marker let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 93E>" funcall puthash if let org-table-widget--measure-string let while let*] 1 [keywordp macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while] 1 [copy-sequence puthash if let org-table-widget--measure-string let while let* if let while let let* org-table-widget--widths let* org-table-widget--render] 4 [cconv-analyze-form cconv-analyze-form cconv-analyze-form cconv-analyze-form cconv-analyze-form cconv--analyze-function cconv-analyze-form cconv-fv cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let*] 1 [set-marker-insertion-type let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 93E>" funcall puthash if let org-table-widget--measure-string max setq progn] 1 [save-excursion let save-current-buffer let "#<interpreted-function 93E>" funcall puthash if let org-table-widget--measure-string let while let* if let while] 1 [copy-sequence puthash if let org-table-widget--measure-string max setq progn if "#<interpreted-function 9A9>" funcall progn let* let org-table-widget--longest-token-pixels let] 3 [<= or org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if] 2 [aref = and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let] 1 [cconv-analyze-form cconv--analyze-function cconv-analyze-form cconv-fv cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let while let] 1 [1+ aref char-width > and org-table-widget--break-after-p and cond let while let progn let* let org-table-widget--longest-token-pixels let] 1 [copy-sequence let "#<interpreted-function 93E>" funcall puthash if let org-table-widget--measure-string let while let* if let while let let*] 2 [let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let* let* let org-table-widget--layout funcall] 1 [let while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths] 4 [if "#<interpreted-function 824>" funcall progn let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths] 1 [let "#<interpreted-function 93E>" funcall puthash if let org-table-widget--measure-string max setq progn if "#<interpreted-function AA9>" funcall progn let* let] 1 [substring org-table-widget--measure-string max setq progn if "#<interpreted-function 33A>" funcall progn let* let org-table-widget--longest-token-pixels let while let* if] 5 [> and org-table-widget--break-after-p and cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if] 1 [cconv--analyze-function cconv-analyze-form cconv-fv cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let while let let*] 1 [if let org-table-widget--measure-string max setq let while let let while let* if let while let let*] 1 [while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let let*] 1 [puthash if let org-table-widget--measure-string max setq progn if "#<interpreted-function 235>" funcall progn let* let org-table-widget--longest-token-pixels let while] 1 [org-table-widget--measure-string max setq progn if "#<interpreted-function 83A>" funcall cond let while let progn let* let org-table-widget--longest-token-pixels let] 1 [delete-region let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 93E>" funcall puthash if let org-table-widget--measure-string let while let*] 2 [setq progn if "#<interpreted-function 279>" funcall cond let while let progn let* let org-table-widget--longest-token-pixels let while let*] 1 [let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect] 1 [copy-sequence let "#<interpreted-function 93E>" funcall puthash if let org-table-widget--measure-string max setq progn if "#<interpreted-function 63A>" funcall progn let*] 4 [funcall cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let] 1 [insert let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 93E>" funcall puthash if let org-table-widget--measure-string max setq progn] 2 [org-table-widget--break-after-p and cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if let while] 2 [cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths let* org-table-widget--render] 2 [macroexp-macroexpand macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let while let let*] 1 [= and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while] 1 [< while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths] 1 [aref char-category-set aref and org-table-widget--break-after-p and cond let while let progn let* let org-table-widget--longest-token-pixels let while] 1 [macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function] 1 [point remove-text-properties let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 93E>" funcall puthash if let org-table-widget--measure-string max setq] 1 [macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths] 1 [nthcdr let* let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let*] 1 [or org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let] 1 [let let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let* let*] 1 [let save-current-buffer let "#<interpreted-function 93E>" funcall puthash if let org-table-widget--measure-string max setq progn if "#<interpreted-function 658>" funcall progn] 1 [funcall puthash if let org-table-widget--measure-string max setq progn if "#<interpreted-function 263A>" funcall progn let* let org-table-widget--longest-token-pixels let] 1 [mapconcat string-join let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render progn unwind-protect let*] 12 [propertize if org-table-widget--pixel-space concat let* org-table-widget--pad let* "#<interpreted-function 804>" apply seq-mapn let let while let let* org-table-widget--row] 5 [window-text-pixel-size car setq let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 93E>" funcall puthash if let org-table-widget--measure-string let*] 6 [mapconcat string-join concat cons setq let let while let let* org-table-widget--row cons setq let* if let] 10 [gethash let org-table-widget--measure-string <= or if org-table-widget--wrap-pixels "#<interpreted-function D5B>" apply seq-mapn let* org-table-widget--row cons setq let* if] 1 [gethash let org-table-widget--measure-string let* org-table-widget--pad let* "#<interpreted-function F65>" apply seq-mapn let let while let let* org-table-widget--row cons] 2 [concat let* "#<interpreted-function AA6>" apply seq-mapn let let while let let* org-table-widget--row cons setq let* if let] 10 [let while let let* org-table-widget--render progn unwind-protect let* let* let org-table-widget--layout funcall let let textui-layout-widget let*] 1 ["#<native-comp-function F616e6f6e796d6f75732d6c616d626461_anonymous_lambda_24>" cconv--analyze-function cconv-analyze-form cconv-fv cconv-make-interpreted-closure function mapcar let* org-table-widget--row cons setq let* if let while let] 1 [if let org-table-widget--measure-string let* org-table-widget--pad let* "#<interpreted-function 00E>" apply seq-mapn let let while let let* org-table-widget--row cons] 1 [if let* "#<interpreted-function A32>" apply seq-mapn let let while let let* org-table-widget--row cons setq let* if let] 1 [<= org-table-widget--regional-indicator-p and if let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends vconcat let* org-table-widget--fit-end let* while let] 1 [cdr mapcar "#<byte-code-function 995>" apply seq-map seq-mapn let* org-table-widget--row cons setq let* if let while let let*] 1 [org-table-widget--measure-string <= or if org-table-widget--wrap-pixels "#<interpreted-function 47D>" apply seq-mapn let* org-table-widget--row cons setq let* if let while] 1 [cdr org-table-widget--pixel-space concat let* org-table-widget--pad let* "#<interpreted-function 82F>" apply seq-mapn let let while let let* org-table-widget--row cons] 1 [let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends vconcat let* org-table-widget--fit-end let* while let if org-table-widget--wrap-pixels "#<interpreted-function 309>" apply] 1 [concat let* org-table-widget--pad let* "#<interpreted-function E5C>" apply seq-mapn let let while let let* org-table-widget--row cons setq let*] 6 [cond org-table-widget--padding-widths let* org-table-widget--pad let* "#<interpreted-function D0B>" apply seq-mapn let let while let let* org-table-widget--row cons setq] 1 [macroexp--expand-all macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function seq-mapn let let while let let* org-table-widget--row] 1 [concat cons setq let let while let let* org-table-widget--row cons setq let* if let while let] 12 [cconv-make-interpreted-closure function mapcar let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render progn unwind-protect] 3 [apply seq-map seq-mapn let let while let let* org-table-widget--row cons setq let* if let while let] 1 [cconv-analyze-form cconv-analyze-form cconv--analyze-function cconv-analyze-form cconv-fv cconv-make-interpreted-closure function mapcar let* org-table-widget--row cons setq let* if let while] 1 [let save-current-buffer org-table-widget--char-pixel-width let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render progn unwind-protect] 1 [let let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render progn unwind-protect let* let*] 1 [let while let let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render progn unwind-protect] 1 ["#<native-comp-function F616e6f6e796d6f75732d6c616d626461_anonymous_lambda_28>" cconv-fv cconv-make-interpreted-closure function seq-mapn let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render] 1 [let save-current-buffer let "#<interpreted-function 93E>" funcall puthash if let org-table-widget--measure-string let* while let* org-table-widget--fit-end let* while let] 1 [let* "#<interpreted-function 8B9>" apply seq-mapn let let while let let* org-table-widget--row cons setq let* if let while] 1 [macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function seq-mapn let let while let let* org-table-widget--row cons] 1 [org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends vconcat let* org-table-widget--fit-end let* while let if] 1 [org-table-widget--measure-string let* org-table-widget--pad let* "#<interpreted-function B3A>" apply seq-mapn let let while let let* org-table-widget--row cons setq let*] 1 ["#<byte-code-function 995>" apply seq-map seq-mapn let let while let let* org-table-widget--row cons setq let* if let while] 1 [cconv-fv cconv-make-interpreted-closure function seq-mapn let let while let let* org-table-widget--row cons setq let* if let while] 1 [window-font-width list let save-current-buffer org-table-widget--font-signature let save-current-buffer org-table-widget--char-pixel-width let* org-table-widget--row cons setq let* if let while] 1 [length < while let org-table-widget--grapheme-ends vconcat let* org-table-widget--fit-end let* while let if org-table-widget--wrap-pixels "#<interpreted-function 37B>" apply seq-mapn] 1 [let* if let while let let* org-table-widget--render progn unwind-protect let* let* let org-table-widget--layout funcall let let] 1 [mapcar "#<byte-code-function 995>" apply seq-map seq-mapn let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render] 1 [mapconcat string-join let let* org-table-widget--render progn unwind-protect let* let* let org-table-widget--layout funcall let let textui-layout-widget let*] 8 [add-face-text-property let let* org-table-widget--render progn unwind-protect let* let* let org-table-widget--layout funcall let let textui-layout-widget let* if] 1 [concat if overlay-put let* if let* org-table-widget--display-table if let while let save-excursion let progn if let] 12 [Automatic\ GC nil] 855)) (27294 32028 367361 0) nil]
