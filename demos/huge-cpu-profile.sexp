
[profiler-profile "28.1" cpu #s(hash-table test equal data ([profiler-start progn condition-case org-table-widget-bench-main apply timer-event-handler nil nil nil nil nil nil nil nil nil nil] 2 ["#<native-comp-function F616e6f6e796d6f75732d6c616d626461_anonymous_lambda_46>" org-element--parse-to org-element-at-point org-at-table.el-p if while save-excursion let org-table-widget--tables let save-excursion let progn if let org-table-widget-refresh] 1 [font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function 491>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure org-table-widget--display-table if let while let save-excursion let progn if] 5 [re-search-forward org-do-emphasis-faces font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function 491>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure org-table-widget--display-table if let while let save-excursion let] 1 [org-fontify-meta-lines-and-blocks-1 org-fontify-meta-lines-and-blocks font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function 491>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure org-table-widget--display-table if let while let save-excursion let] 1 [get-char-property org-fold-core-get-folding-spec org-activate-folds font-lock-fontify-keywords-region font-lock-default-fontify-region font-lock-fontify-region "#<byte-code-function 491>" jit-lock--run-functions jit-lock-fontify-now font-lock-ensure org-table-widget--display-table if let while let save-excursion] 1 [line-end-position let org-table-widget--line-cells let cond while save-excursion let org-table-widget--parse and let* org-table-widget--display-table if let while let] 5 [progn if while let* let* while let let org-table-widget--clean-cell cons setq while let progn if save-excursion] 1 [nreverse let* let* while let let org-table-widget--clean-cell cons setq while let progn if save-excursion let org-table-widget--line-cells] 1 [prog1 car-safe setq progn while let* let* while let let org-table-widget--clean-cell cons setq while let progn] 1 [while let* let* while let let org-table-widget--clean-cell cons setq while let progn if save-excursion let org-table-widget--line-cells] 1 [looking-at looking-at-p cond while save-excursion let org-table-widget--parse and let* org-table-widget--display-table if let while let save-excursion let] 1 [memq if while let* let* while let let org-table-widget--clean-cell cons setq while let progn if save-excursion] 1 [text-properties-at let* while let let org-table-widget--clean-cell cons setq while let progn if save-excursion let org-table-widget--line-cells let] 1 [< if let progn if save-excursion let org-table-widget--line-cells let cond while save-excursion let org-table-widget--parse and let*] 1 [string-trim let org-table-widget--clean-cell cons setq while let progn if save-excursion let org-table-widget--line-cells let cond while save-excursion] 4 [re-search-forward while let progn if save-excursion let org-table-widget--line-cells let cond while save-excursion let org-table-widget--parse and let*] 2 [setq progn while let* let* while let let org-table-widget--clean-cell cons setq while let progn if save-excursion] 1 [buffer-substring org-table-widget--clean-cell cons setq while let progn if save-excursion let org-table-widget--line-cells let cond while save-excursion let] 2 [cconv--analyze-function cconv-analyze-form cconv-fv cconv-make-interpreted-closure function seq-some and org-table-widget--cookie-row-p if let cond while save-excursion let org-table-widget--parse and] 1 [cconv-make-interpreted-closure function seq-some and org-table-widget--cookie-row-p if let cond while save-excursion let org-table-widget--parse and let* org-table-widget--display-table if] 1 [let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let* let* let] 2 [substring org-table-widget--measure-string max setq let while let let while let* if let while let let* org-table-widget--widths] 25 [substring org-table-widget--measure-string max setq progn if "#<interpreted-function FB2>" funcall cond let while let progn let* let org-table-widget--longest-token-pixels] 2 [copy-sequence let "#<interpreted-function 70B>" funcall puthash if let* org-table-widget--measure-string max setq progn if "#<interpreted-function 4C1>" funcall progn let*] 3 [prin1-to-string let* org-table-widget--measure-string max setq progn if "#<interpreted-function 84B>" funcall progn let* let org-table-widget--longest-token-pixels let while let*] 15 [window-text-pixel-size car setq let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 70B>" funcall puthash if let* org-table-widget--measure-string let] 58 [prin1-to-string let* org-table-widget--measure-string max setq let while let let while let* if let while let let*] 202 [cconv-fv cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths let*] 2 [prin1-to-string let* org-table-widget--measure-string max setq progn if "#<interpreted-function 47B>" funcall cond let while let progn let* let] 39 [prin1-to-string let* org-table-widget--measure-string let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect] 16 [quote gethash let* org-table-widget--measure-string max setq progn if "#<interpreted-function 3A1>" funcall progn let* let org-table-widget--longest-token-pixels let while] 1 [max setq let while let let while let* if let while let let* org-table-widget--widths let* org-table-widget--render] 2 [let while let let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect] 6 [substring org-table-widget--measure-string max setq progn if "#<interpreted-function 041>" funcall progn let* let org-table-widget--longest-token-pixels let while let* if] 4 [eq if let* org-table-widget--measure-string max setq progn if "#<interpreted-function F66>" funcall cond let while let progn let*] 1 [puthash if let* org-table-widget--measure-string max setq progn if "#<interpreted-function 90E>" funcall progn let* let org-table-widget--longest-token-pixels let while] 4 [let* org-table-widget--measure-string max setq let while let let while let* if let while let let* org-table-widget--widths] 2 [while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let let*] 1 [window-text-pixel-size car setq let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 70B>" funcall puthash if let* org-table-widget--measure-string max] 55 [memq cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let] 2 [aref memq cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if let while] 2 [length < while let org-table-widget--grapheme-ends let let while let* if let while let let* org-table-widget--widths let*] 3 [let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let let* org-table-widget--widths] 2 [delete-region let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 70B>" funcall puthash if let* org-table-widget--measure-string let while let*] 2 [and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let] 3 [cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths let* org-table-widget--render] 1 [let* let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let* let*] 2 [1+ aref char-width > and org-table-widget--break-after-p and cond let while let progn let* let org-table-widget--longest-token-pixels let] 1 [funcall cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let] 1 [car let while let let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn] 2 [let "#<interpreted-function 70B>" funcall puthash if let* org-table-widget--measure-string let while let* if let while let let* org-table-widget--widths] 2 [cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let let*] 1 [length let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let let*] 1 [puthash if let* org-table-widget--measure-string let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn] 5 [setq while let org-table-widget--grapheme-ends let let while let* if let while let let* org-table-widget--widths let* org-table-widget--render] 1 [org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let* let*] 1 [aref char-category-set aref and org-table-widget--break-after-p and cond let while let progn let* let org-table-widget--longest-token-pixels let while] 1 [let save-current-buffer let "#<interpreted-function 70B>" funcall puthash if let* org-table-widget--measure-string max setq progn if "#<interpreted-function CA9>" funcall progn] 3 [progn if "#<interpreted-function 2A9>" funcall cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if] 1 [let while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths] 3 [put-text-property let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 70B>" funcall puthash if let* org-table-widget--measure-string max setq progn] 2 [add-face-text-property let "#<interpreted-function 70B>" funcall puthash if let* org-table-widget--measure-string max setq progn if "#<interpreted-function CA6>" funcall progn let*] 1 [1- < and cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if let] 2 [while let org-table-widget--grapheme-ends let let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn] 1 [quote eq if let* org-table-widget--measure-string max setq let while let let while let* if let while] 1 [copy-sequence let "#<interpreted-function 70B>" funcall puthash if let* org-table-widget--measure-string let while let* if let while let let*] 4 [aref = and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let] 2 [< and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while] 2 [char-width = or org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let*] 1 [let save-current-buffer let "#<interpreted-function 70B>" funcall puthash if let* org-table-widget--measure-string let while let* if let while let] 3 [<= or org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if] 1 [org-table-widget--measure-string max setq let while let let while let* if let while let let* org-table-widget--widths let*] 4 [if let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let let*] 2 [aref org-table-widget--regional-indicator-p and if let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let] 2 [gethash let* org-table-widget--measure-string max setq let while let let while let* if let while let let*] 5 [quote remove-text-properties let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 70B>" funcall puthash if let* org-table-widget--measure-string let while] 1 ["#<interpreted-function D8E>" funcall progn let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths let*] 1 [insert let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 70B>" funcall puthash if let* org-table-widget--measure-string let while let*] 2 [1+ setq while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let let*] 1 [let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 70B>" funcall puthash if let* org-table-widget--measure-string max setq progn if] 1 [macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let while let let*] 1 [set-marker-insertion-type let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 70B>" funcall puthash if let* org-table-widget--measure-string let while let*] 1 [org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while] 3 [org-table-widget--break-after-p and cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if let while] 3 [macroexpand-1 macroexp-macroexpand macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let*] 1 [gethash let* org-table-widget--measure-string max setq progn if "#<interpreted-function C0E>" funcall cond let while let progn let* let] 1 [setq while let progn let* let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths] 1 [= or org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if] 1 [macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let] 1 [insert let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 70B>" funcall puthash if let* org-table-widget--measure-string max setq progn] 1 [macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let*] 1 [delete-region let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 70B>" funcall puthash if let* org-table-widget--measure-string max setq progn] 1 [eq if let* org-table-widget--measure-string max setq let while let let while let* if let while let] 1 [char-category-set aref and org-table-widget--break-after-p and cond let while let progn let* let org-table-widget--longest-token-pixels let while let*] 3 [org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while let let* org-table-widget--widths let*] 1 [let org-table-widget--longest-token-pixels let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let*] 1 [cconv-analyze-form cconv-analyze-form cconv--analyze-function cconv-analyze-form cconv-fv cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let while] 1 [aref org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let] 1 [org-table-widget--measure-string max setq progn if "#<interpreted-function 4F2>" funcall cond let while let progn let* let org-table-widget--longest-token-pixels let] 1 [org-table-widget--regional-indicator-p and if let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let while] 1 ["#<native-comp-function F616e6f6e796d6f75732d6c616d626461_anonymous_lambda_24>" cconv--analyze-function cconv-analyze-form cconv-fv cconv-make-interpreted-closure function let* let org-table-widget--longest-token-pixels let while let* if let while let] 1 [<= org-table-widget--regional-indicator-p and if let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends let let while let* if let] 1 [nthcdr let* let while let* if let while let let* org-table-widget--widths let* org-table-widget--render progn unwind-protect let*] 1 [aref and org-table-widget--break-after-p and cond let while let progn let* let org-table-widget--longest-token-pixels let while let* if] 1 [macroexpand-1 macroexp-macroexpand macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all] 1 [concat cons setq let let while let let* org-table-widget--row cons setq let* if let while let] 11 [window-text-pixel-size car setq let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 70B>" funcall puthash if let* org-table-widget--measure-string let*] 4 [prin1-to-string let* org-table-widget--measure-string <= or if org-table-widget--wrap-pixels "#<interpreted-function 510>" apply seq-mapn let* org-table-widget--row cons setq let* if] 16 [mapconcat string-join let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render progn unwind-protect let*] 15 [prin1-to-string let* org-table-widget--measure-string let* while let* org-table-widget--fit-end let* while let if org-table-widget--wrap-pixels "#<interpreted-function 6BB>" apply seq-mapn let*] 8 [mapconcat string-join concat cons setq let let while let let* org-table-widget--row cons setq let* if let] 12 [let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends vconcat let* org-table-widget--fit-end let* while let if org-table-widget--wrap-pixels "#<interpreted-function 40E>" apply] 1 [macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function mapcar let* org-table-widget--row cons] 1 [concat let* "#<interpreted-function 60E>" apply seq-mapn let let while let let* org-table-widget--row cons setq let* if let] 11 [char-width = or org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends vconcat let* org-table-widget--fit-end let*] 1 ["#<interpreted-function E7D>" apply seq-mapn let let while let let* org-table-widget--row cons setq let* if let while let] 1 [kill-buffer "#<byte-code-function 8C3>" substitute-command-keys let* org-table-widget--measure-string let* org-table-widget--pad let* "#<interpreted-function AAF>" apply seq-mapn let let while let let*] 5 [let* org-table-widget--measure-string let* org-table-widget--pad let* "#<interpreted-function AAF>" apply seq-mapn let let while let let* org-table-widget--row cons setq] 13 [let save-current-buffer let "#<interpreted-function 70B>" funcall puthash if let* org-table-widget--measure-string let* while let* org-table-widget--fit-end let* while let] 1 [window-font-width progn condition-case let save-current-buffer org-table-widget--char-pixel-width let* org-table-widget--row cons setq let* if let while let let*] 3 [concat let* org-table-widget--pad let* "#<interpreted-function 474>" apply seq-mapn let let while let let* org-table-widget--row cons setq let*] 7 [prin1-to-string let* org-table-widget--measure-string let* org-table-widget--pad let* "#<interpreted-function 574>" apply seq-mapn let let while let let* org-table-widget--row cons] 18 [seq-map seq-mapn let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render progn unwind-protect let*] 1 [aref org-table-widget--regional-indicator-p and if let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends vconcat let* org-table-widget--fit-end let* while let] 1 [gethash let* org-table-widget--measure-string <= or if org-table-widget--wrap-pixels "#<interpreted-function 773>" apply seq-mapn let* org-table-widget--row cons setq let* if] 1 [propertize if org-table-widget--pixel-space concat let* org-table-widget--pad let* "#<interpreted-function 209>" apply seq-mapn let let while let let* org-table-widget--row] 5 ["#<byte-code-function 995>" apply seq-map seq-mapn let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render progn] 1 [eq cond org-table-widget--padding-widths let* org-table-widget--pad let* "#<interpreted-function ECE>" apply seq-mapn let let while let let* org-table-widget--row cons] 1 [seq--into-list seq-into "#<byte-code-function A3D>" mapcar "#<byte-code-function 995>" apply seq-map seq-mapn let let while let let* org-table-widget--row cons setq] 1 [cconv-make-interpreted-closure function seq-mapn let let while let let* org-table-widget--row cons setq let* if let while let] 1 [let* org-table-widget--fit-end let* while let if org-table-widget--wrap-pixels "#<interpreted-function 8BD>" apply seq-mapn let* org-table-widget--row cons setq let* if] 1 [let* org-table-widget--measure-string <= or if org-table-widget--wrap-pixels "#<interpreted-function F90E>" apply seq-mapn let* org-table-widget--row cons setq let* if let] 1 [org-table-widget--measure-string <= or if org-table-widget--wrap-pixels "#<interpreted-function 7A6>" apply seq-mapn let* org-table-widget--row cons setq let* if let while] 1 [org-table-widget--padding-widths let* org-table-widget--pad let* "#<interpreted-function 009>" apply seq-mapn let let while let let* org-table-widget--row cons setq let*] 2 [macroexpand-1 macroexp-macroexpand macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function seq-mapn let* org-table-widget--row cons setq let*] 1 [prin1-to-string let* org-table-widget--measure-string let org-table-widget--boundary let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render] 1 [delete-dups cconv-fv cconv-make-interpreted-closure function seq-mapn let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render] 1 [cconv-analyze-form cconv-analyze-form cconv--analyze-function cconv-analyze-form cconv-fv cconv-make-interpreted-closure function seq-mapn let let while let let* org-table-widget--row cons setq] 1 [display-multi-font-p window-font-width progn condition-case let save-current-buffer org-table-widget--char-pixel-width let* org-table-widget--row cons setq let* if let while let] 1 [insert let save-restriction save-excursion let save-current-buffer let "#<interpreted-function 70B>" funcall puthash if let* org-table-widget--measure-string let* while let*] 1 [org-table-widget--wrap-pixels "#<interpreted-function E0E>" apply seq-mapn let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render progn] 2 [macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function mapcar let* org-table-widget--row cons setq let* if] 1 [org-table-widget--grapheme-extension-p and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends vconcat let* org-table-widget--fit-end let* while let if] 2 [macroexp-macroexpand macroexp--expand-all macroexp--all-forms macroexp--all-clauses macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function seq-mapn let let while let let*] 1 [cons setq while let org-table-widget--grapheme-ends vconcat let* org-table-widget--fit-end let* while let if org-table-widget--wrap-pixels "#<interpreted-function B65>" apply seq-mapn] 1 [substring org-table-widget--measure-string let* while let* org-table-widget--fit-end let* while let if org-table-widget--wrap-pixels "#<interpreted-function B74>" apply seq-mapn let* org-table-widget--row] 1 [cconv--analyze-function cconv-analyze-form cconv-fv cconv-make-interpreted-closure function seq-mapn let let while let let* org-table-widget--row cons setq let* if] 1 [macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexp--all-forms macroexp--all-clauses macroexp--expand-all macroexp--all-forms macroexp--expand-all macroexpand-all cconv-make-interpreted-closure function seq-mapn let let while let] 1 [copy-sequence let "#<interpreted-function 70B>" funcall puthash if let* org-table-widget--measure-string let* while let* org-table-widget--fit-end let* while let if] 1 [concat let org-table-widget--boundary let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render progn unwind-protect] 1 ["#<interpreted-function C35>" mapcar let* org-table-widget--row cons setq let* if let while let let* org-table-widget--render progn unwind-protect let*] 1 [and while let* org-table-widget--next-grapheme-end setq while let org-table-widget--grapheme-ends vconcat let* org-table-widget--fit-end let* while let if org-table-widget--wrap-pixels] 1 [mapconcat string-join let let* org-table-widget--render progn unwind-protect let* let* let org-table-widget--layout funcall let let textui-layout-widget let*] 6 [concat if overlay-put let* if let* org-table-widget--display-table if let while let save-excursion let progn if let] 15 [Automatic\ GC nil] 847)) (27294 30793 112955 0) nil]
